{
  "name": "dst-broadcasts",
  "type": "module",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "pack": "bun run tools/release.ts --pack-only",
    "release": "bun run tools/release.ts",
    "release:dry": "bun run tools/release.ts --dry-run"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
