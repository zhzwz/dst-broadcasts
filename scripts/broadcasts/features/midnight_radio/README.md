# features/midnight_radio（午夜电台）

**黄昏 → 黑夜**时开播。**始终开启**，无配置项。

## 触发

- 森林：`phase` dusk → night
- 洞穴：`cavephase` dusk → night
- 读档停在黑夜、或相位尚未就绪时不播

## 节目

`OnAir()` 目前为空，待填午夜电台内容。

## 依赖

- `BROADCASTS_SAFE` / `BROADCASTS_DAY_SLOT`

`README.md` 仅供仓库阅读；打包时会排除，不进工坊包。
